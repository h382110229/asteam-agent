# ASTeam Agent v1.8.3 详尽全功能测试用例与验证手册 (Test Cases Manual)

> **版本**：v1.8.3 (Build 20260912)  
> **核心定位**：出境安全前置交互预检、三态弹性放行与企业审计追踪。  
> **验证执行方式**：
> - 自动集成测试：`npm run test:verify` (`scripts/verify-v1.8.3.mjs`)
> - 桌面安装包：`release/ASTeam Agent-Setup-1.8.3.exe`
> - 桌面便携版：`release/ASTeam Agent-Portable-1.8.3.exe`

---

## 目录
1. [第一部分：自动化测试套件矩阵 (176 项全通过)](#第一部分自动化测试套件矩阵)
2. [第二部分：v1.8.3 核心新特性人工端到端 (E2E) 实机用例](#第二部分v183-核心新特性人工端到端实机用例)
3. [第三部分：场景 7 敏感测试资产实机回测指导](#第三部分场景-7-敏感测试资产实机回测指导)

---

# 第一部分：自动化测试套件矩阵

运行自动化测试命令：
```bash
npm run test:verify
```

### 测试组统计一览
- **Test Group 1**: 纯 JS 原生 Office 排版套件 (Word/Excel/PPT) — 12 项 (✅ PASS)
- **Test Group 2**: 交付物可信验证门禁 (物理校验/陈旧拦截/零字节报警) — 12 项 (✅ PASS)
- **Test Group 3**: Windows 终端沙箱与命令执行鲁棒性 — 8 项 (✅ PASS)
- **Test Group 4**: 长期持久记忆库与自我演化技能 — 15 项 (✅ PASS)
- **Test Group 5**: StorageHub 闭环与 D 盘优先存储 — 14 项 (✅ PASS)
- **Test Group 6**: 路径安全隔离与重定向 — 18 项 (✅ PASS)
- **Test Group 7**: 零外部依赖工具链 (PDF/ZIP/锁自愈/Git) — 19 项 (✅ PASS)
- **Test Group 8**: Skill & MCP 深度融合 (npx注入/Schema校验/微索引/纯JS工具) — 33 项 (✅ PASS)
- **Test Group 9**: 步骤状态流转优化与 UI 取消失步修复 — 9 项 (✅ PASS)
- **Test Group 10**: Grill-me 多题结构化交互与问卷提取 — 8 项 (✅ PASS)
- **Test Group 11**: Word 嵌套代码块提取与静态 TOC 生成 (零 Word 告警) — 10 项 (✅ PASS)
- **Test Group 12 (v1.8.3 新增)**: 出境安全前置交互预检与三态弹性放行 — 18 项 (✅ PASS)
- **总计**：176 项全量通过（0 失败）。

---

# 第二部分：v1.8.3 核心新特性人工端到端 (E2E) 实机用例

### 用例 1：出境安全前置交互预检弹窗唤起与敏感资产统计
- **前置条件**：启动客户端（`release/ASTeam Agent-Portable-1.8.3.exe` 或 `npm run dev`）。
- **测试输入**：
  ```
  请帮我规划一下将数据库迁移到内网服务器 192.168.1.120 的方案，密码暂时配置为 postgres://root:Secr3tP@ss@192.168.1.120:5432/core
  ```
- **预期结果**：
  1. 点击发送或回车瞬间，输入框不上屏发送，立刻弹出「🛡️ 出境流量敏感特征交互预检」拟态弹窗；
  2. 弹窗清晰列出检测到的敏感资产：`内网私有 IP (192.168.x)`（2 处）、`数据库连接口令`（1 处）；
  3. 提供「🛡️ 脱敏后发送 (推荐)」、「⚡ 原样发出 (本次豁免)」与「✏️ 返回修改」三个按钮；
  4. 支持展开「脱敏对比视图」，左侧红框高亮原始敏感词，右侧绿框展示脱敏占位符。

---

### 用例 2：脱敏后发送 (推荐路径) 验证
- **测试操作**：在用例 1 弹窗中点击「🛡️ 脱敏后发送 (推荐)」。
- **预期结果**：
  1. 弹窗关闭；
  2. 消息流中显示的用户消息已替换为脱敏占位符（如 `<PRIVATE_IP_REDACTED_1>` 与 `<DB_PASSWORD_REDACTED_1>`）；
  3. Agent 正常接收脱敏后的内容，生成迁移规划，无任何硬阻断报错；
  4. 思考流显示 `🛡️ [企业安全围栏] 已对请求出境文本实施实时脱敏保护`。

---

### 用例 3：原样发出 (本次豁免) 与合规审计记录验证
- **测试操作**：
  1. 输入包含私有 IP 的排障指令：`请测试 192.168.3.50 的连通性`；
  2. 弹窗出现后，点击「⚡ 原样发出 (本次豁免)」。
- **预期结果**：
  1. 弹窗关闭，消息原样发出（保留 `192.168.3.50`）；
  2. Agent 思考流中输出提示：`> ⚡ [出境安全豁免已生效] 本次请求已根据用户授权原样发送至模型（已留存合规审计日志）`；
  3. 打开顶部导航「企业合规」中枢，在审计日志表格中可观察到一条新增的 `豁免放行` 记录，原文本已被掩码处理，放行原因为 `用户发送前主动确认豁免放行 (Bypassed by User)`。

---

### 用例 4：当前会话偏好记忆 (Session Remember) 验证
- **测试操作**：
  1. 输入包含私有 IP 的消息：`ping 192.168.1.88`；
  2. 在弹出的预检弹窗中，勾选「在当前会话中记住此选择」，并点击「原样发出 (本次豁免)」；
  3. 在同一会话中再次发送包含敏感资产的消息：`telnet 192.168.1.99 8080`。
- **预期结果**：
  1. 第二次发送时零弹窗打扰，自动继承会话豁免偏好，直接原样发出；
  2. 点击左侧侧边栏新建一个「新对话」，在新对话中输入 `ping 192.168.1.66`；
  3. 新对话中预检弹窗如期重新出现，严格防护跨会话敏感泄露。

---

# 第三部分：场景 7 敏感测试资产实机回测指导

针对此前在场景 7 触发安全围栏导致任务停滞的问题，现已全链路修复闭环。用户可在本客户端直接执行如下回测：

**回测指令**：
> 请帮我分析我们网络中 `192.168.1.100` 与 `192.168.1.200` 之间的流量转发规则，并给出测试建议。

**体验升级点**：
1. **不再硬拦截**：发送时直接通过拟态卡片预先征询您的决策；
2. **选择脱敏发出**：系统安全脱敏后大模型无缝规划完成；
3. **选择豁免发出**：真实 IP 原样传达至大模型，且思考流不再覆盖模型回复，全流程平滑流畅！
