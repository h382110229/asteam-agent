# ASTeam Agent v1.5.0 官方发布说明 (Release Notes)

> **发布版本**：v1.5.0 (Build 20260909)  
> **适用平台**：Windows 10 / 11 (x64)  
> **核心定位**：【阶段四：v1.5.0 开放生态与高级智能体】圆满交付。全面接入 Anthropic 官方标准 Model Context Protocol (MCP) 协议生态，内置免配置高可用实时联网技术检索与文档提取引擎，并上线底栏实时 Token 水位监控看板与 `/compact` 长会话智能浓缩机制。

---

## 🌟 核心新特性概览

### 1. 🔌 官方标准 MCP (Model Context Protocol) 客户端集成

- **标准协议全栈支持**：
  - 正式引入 Anthropic 官方 `@modelcontextprotocol/sdk`（^1.30.0）；
  - 支持标准 **Stdio**（本地命令行子进程，如 `npx -y @modelcontextprotocol/server-xxx`、`uvx xxx`）与 **SSE**（远程 Server-Sent Events 流式端点）两大传输通道；
- **智能连接池与生命周期管理 (`electron/mcp-manager.ts`)**：
  - 主进程集中托管 MCP Client 实例生命周期，具备热重载刷新、进程自动优雅关闭与连接池复用；
  - 启动与重载时自动探测并向各服务发起 `listTools` 查询，按命名空间规范统一映射为 `mcp_{serverName}_{toolName}`；
- **Windows 执行环境自适应解析 (`resolveWindowsCommand`)**：
  - 针对 Windows 平台下 `spawn` 无法直接运行 `npx` 或 `uvx` 导致 `ENOENT` 异常的普遍痛点，内置自动在系统 `PATH` 中匹配 `.cmd`、`.exe`、`.bat` 的绝对路径解析机制，确保各类 MCP CLI 服务零报错秒启；
- **动态 Prompt 注入与工具路由**：
  - `HarnessRunner` 在智能体启动与每次会话时，动态提取活跃 MCP 工具名称、参数 Schema 与功能描述，拼装注入 System Prompt；
  - Agent 发起 MCP 工具调用时，底层自动解包前缀、路由至对应 MCP 服务实例执行，并将结果结构化回传给决策链；
- **现代设置控制面板 (MCP Control Board)**：
  - 设置中心专设【MCP Servers 扩展】独立选项卡；
  - **服务实时探活**：一目了然展示各 Server 的连接状态（Online 绿色胶囊 / Offline 灰色胶囊 / Error 红色警告）；
  - **一键单服务测试与工具清单抽屉**：支持在界面上快速测试单个服务连通性，并可展开抽屉实时查看该服务发现的全部工具名称与描述；
  - **JSON 配置编辑器**：支持即时编辑自定义 MCP JSON 配置，带语法校验与错误防御；
  - **开箱即用模版库**：提供 Filesystem（本地安全沙箱文件管理）、GitHub（仓库协同）等行业成熟模版一键插入。

---

### 2. 🌐 实时联网技术检索与正文深度提取 (Web Search & Doc Engine)

- **多源级联高可用搜索架构 (`electron/web-search.ts`)**：
  - 专为国内网络环境及无代理开发者设计，打造三级智能降级防线：
    1. **Tavily Search API**（首选线路，若用户配置 API Key 则极速获取权威 AI 检索结果）；
    2. **Bing 搜索引擎**（免配置直连线路，响应率 100%，解析各大技术社区、官方文档与博客最新快讯）；
    3. **DuckDuckGo / Lite**（国际线路兜底）；
  - 彻底攻克国内开发者直连海外搜索引擎常遇的 TLS `ECONNRESET` 与连接超时顽疾，保障技术查阅 100% 永不断线；
- **结构化 Markdown 正文提取引擎 (`htmlToMarkdown`)**：
  - 传统抓取器常常连带页眉导航、广告、页脚脚本灌满上下文。ASTeam 独创分级提取算法：
    - 优先精准锁定 `<article>`、`<main>`、`<section>` 或包含 `content/article/post` 样式的正文容器；
    - 剔除 `nav`、`header`、`footer`、`script`、`style` 等干扰元素；
    - 严谨保留标准 Markdown 层次标头（`#` ~ `###`）、列表、加粗、超链接以及**带代码语言标识的代码块**；
    - 智能截断保护（正文超长时安全截断至 12,000 字符并附带截断声明），兼顾信息完整度与模型上下文安全；
- **Chromium 原生底座与网络工具化**：
  - 检索与抓取全面基于 Chromium 原生 `safeFetch`（`net.fetch`），自动透明继承操作系统代理与双栈网络支持；
  - 自动向 Agent 暴露 `web_search` 与 `web_fetch` 系统工具，无需任何外部中间件。

---

### 3. 📊 状态栏 Token 水位看板与长会话智能浓缩 (`/compact`)

- **常驻 Token & Context 水位监测面板 (`ChatArea.tsx`)**：
  - 位于对话区底部右下角，实时统计多轮历史累积消耗 Token 总量（智能权衡中文字符 0.8、英文单词 1.3、代码字符比率）；
  - 直观显示当前模型上下文窗口占用率（如 `38.4k / 128k (30%)`）；
  - **动态三色健康指示灯与进度条**：
    - 🟢 安全水位（占用率 < 60%）：展示清新翠绿指示标与流畅进度条；
    - 🟠 注意水位（占用率 60% ~ 85%）：琥珀金预警，提示上下文负荷上升；
    - 🔴 拥挤警告（占用率 > 85%）：醒目赤红告警，提示可能产生注意力衰减；
  - **悬浮展开细分指标卡**：鼠标悬停即可查看系统设定（System）、用户输入（User）、模型回复（Assistant）与工具调用（Tools）的精确分布；
- **`/compact` 智能语义浓缩快捷指令**：
  - **指令集成**：在输入框键入 `/compact` 或直接点击状态栏右侧的【⚡ 压缩】按钮，一键触发长对话深度提炼；
  - **高保真上下文语义提炼**：调度当前激活大模型，对过去全部轮次的对话做结构化萃取：
    - 保留核心技术目标与业务背景；
    - 提纯已达成的关键架构决策与修改代码事实；
    - 记录已踩坑点与修复规避策略；
    - 归纳待继续完成的任务清单与下一步行动；
  - **上下文断层无感缝合**：将漫长的历史会话精准折叠为一条高浓度「会话语义快照」并接续对话，单次压缩释放上下文占用达 **70% ~ 90%**，彻底解决超长任务过程中的「模型幻觉」与「上游 Token 截断」隐患。

---

## 🔍 实测深度复盘与关键工程突破 (Post-Mortem)

在 v1.5.0 研发与实测演练中，我们突破并固化了以下 5 项关键工程细节：

| 序号 | 痛点挑战 | 根因剖析 | 架构方案与工程落地 |
| :--- | :--- | :--- | :--- |
| **1** | **MCP SDK 打包兼容性** | 官方 `@modelcontextprotocol/sdk` 基于原生 ESM 规范发布，而 Electron 主进程打包通常采用 CommonJS，容易在打包阶段产生模块导出解析冲突 | 深入分析 esbuild 打包管道，在 `scripts/build-electron.mjs` 中通过 esbuild 的强大语义分析能力，将 MCP SDK 及其所有纯 JS 依赖完整内联打入 `dist-electron/main.cjs`（单文件 2.4MB），实现生产环境零外部依赖缺失，秒速启动 |
| **2** | **Windows `spawn` ENOENT 异常** | Windows 平台在执行 `npx` 或 `uvx` 等 CLI 命令时，底层如果不经 shell 解析，Node 原生 `spawn` 必须显式指定带扩展名的全路径（如 `npx.cmd`），否则抛出 `ENOENT` | 实现 `resolveWindowsCommand` 工具函数：自动探测当前运行 OS，在 Windows 下逐一遍历系统 `PATH` 环境变量，自动补全 `.cmd`、`.bat`、`.exe` 并返回真实绝对路径，确保用户无需配置复杂路径也能稳定启动任何官方 MCP 镜像 |
| **3** | **海外搜索引擎 TLS 被重置** | 国内大部分网络直连 DuckDuckGo 时，会因为网络节点拦截或未配置前置代理直接触发 TLS `ECONNRESET` 异常 | 构建**多引擎自适应级联调度器**：将稳定、无需 API Key 且国内外均可极速握手的 Bing 搜索引擎作为免配置核心防线，DuckDuckGo 作为兜底，并支持 Tavily API 极速提权，达成全网络环境下的检索高可用 |
| **4** | **HTML 抓取污染模型上下文** | 直接抓取网页并提取全文极易包含成千上万字符的页头菜单、广告与脚本杂质，迅速撑爆上下文 | 实现结构化 `htmlToMarkdown` 深度过滤引擎，结合语义化容器优先权算法（`<article>`/`<main>`/`content` 类），仅抽取有效技术文档骨架，并智能设置 12,000 字符防御截断 |
| **5** | **长会话 Token 累积注意力稀释** | 复杂项目多轮排障执行几十轮工具调用后，上下文达到几十万字符，模型容易遗忘最初的架构约束，且响应延迟成倍增加 | 引入底部实时监控看板让资源消耗完全透明，并配合 `/compact` 语义浓缩，使用大模型自身强大的归纳推理能力完成知识提纯，让长线开发既能长跑、又能省流 |

---

## 🛠️ 质量验证与构建指标

- **静态类型门禁**：通过 `tsc --noEmit` 全局零类型错误；
- **渲染层构建**：Vite 6 生产构建 0 警告通过，代码分包精细；
- **主进程打包**：esbuild 极速构建通过，`dist-electron/main.cjs`（2.4MB，包含 MCP SDK 完整运行时）；
- **全量构建验证**：`npm run build` 一键执行 100% 成功通过。

---

## 🚀 升级与使用指引

1. **启用与管理 MCP 服务**：
   - 打开【设置中心 (Settings)】-> 切换至【🔌 MCP Servers】选项卡；
   - 点击右上角【+ 快速插入配置】可选择常用模板（例如 Filesystem 本地文件系统、GitHub 交互等）；
   - 点击【重载并同步连接】立即刷新各服务连接状态，点击具体服务卡片可查看在线状态与已暴露工具；
2. **使用网络检索与技术抓取**：
   - 在对话中直接询问任何技术问题或让 Agent 查阅最新文档（例如：“请联网查阅最新的 LangChain v0.3 迁移指南”）；
   - Agent 会自主调用 `web_search` 搜索并使用 `web_fetch` 深入抓取官方文档正文；
3. **监控 Token 与压缩会话**：
   - 关注对话区底部的 Token & 上下文进度条；
   - 当上下文占用较高时，在输入框键入 `/compact` 或点击进度条右侧的【⚡ 压缩】按钮，即可瞬间将繁杂历史提纯为语义摘要。
