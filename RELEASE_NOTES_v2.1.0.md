# ASTeam Agent v2.1.0 发版说明 (Release Notes)

### 🌟 核心升级特性与交互重塑

1. **[体验重塑] 现代化 GFM Markdown 回显渲染引擎**
   - 彻底告别旧版纯文本换行输出，引入标准 GitHub Flavored Markdown 渲染管道。
   - **表格卡片化**：多列配置梳理表、打流用例表自动渲染为带圆角边框、表头悬浮、斑马纹高亮与横向平滑滚动的卡片式表格，彻底杜绝管道符 `|` 错位变形。
   - **高对比代码块**：集成语法高亮、语言类别标签识别（Python, Shell, JSON, TypeScript 等）以及右上角「一键复制」剪贴板交互。
   - **层级与排版**：全面规范 H1–H4 标题阶梯、列表项与引用块，提升长文档阅读体验。

2. **[交付闭环] 物理探针贯通与多交付物聚合看板 (Multi-Artifacts Delivery Board)**
   - 后端物理探针（`ArtifactVerifier`）核验通过的真实落地文件作为结构化数据一等公民回传给前端。
   - 聊天消息结论底部新增**「交付物清单聚合看板」**，无论是 1 个还是 8 个文件（如 7 个 XLSX + 1 个 TXT），均以网格清单集中呈现。
   - 每项交付物均附带格式专属图标（Excel 绿、Word 蓝、TXT 灰、PDF 红）、文件字节体积、时间戳。
   - 提供「在资源管理器中定位 (Reveal)」、「复制绝对物理路径」、「工作台极速分屏预览」等高频快捷操作。

3. **[工作台升级] Excel 表格与脚本代码极速分屏预览**
   - **Excel 表格快速预览**：内置轻量解析器，右侧工作台即可直接检视 XLSX/CSV 表格内容，支持多 Sheet 标签切换、表格网格展示、数据行即时搜索，无需打开臃肿的本地 Office。
   - **文本与配置预览**：支持查看导出的防火墙配置脚本 `.txt`、JSON、Python 脚本等，带代码行号与全选复制。
   - **交付物货架全面收纳**：新增「Excel 表格」与「代码/文本」分类过滤 Chips，全面接纳由自动化外部脚本批量落盘的所有产物。

4. **[底层修复] Windows PowerShell 控制台 UTF-8 原生编码修正**
   - 强制为 PowerShell 子进程注入 UTF-8 代码页声明，并配合 `StringDecoder` 防止中文字符多字节截断。
   - 彻底根除 `? _ʾ...` 菱形乱码，确保包含中文路径和文件名的输出在控制台与回显中 100% 准确清晰。
   - 引入长任务活跃度保活（Activity Heartbeat），避免长耗时的管线批处理被 60s 硬超时误杀。

5. **[交互进阶] 深度思考流 (Thought Stream) 现代化折叠胶囊**
   - 替换原生 `<details>` 标签，升级为带有呼吸动效的思维链折叠胶囊。
   - 思考中动态展示状态，正文生成后优雅折叠，带字符统计与一键平滑收起展开。
