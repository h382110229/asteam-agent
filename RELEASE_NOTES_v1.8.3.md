# ASTeam Agent v1.8.3 官方发布说明 (Release Notes)

> **发布版本**：v1.8.3 (Build 20260912)  
> **代码基线**：主分支 `main`  
> **核心定位**：【阶段七点三：v1.8.3 出境安全前置交互预检与三态弹性放行】全面圆满交付。彻底颠覆传统“发出后硬拦截/静默覆写”的生硬安全机制，引入基于前端轻量扫描的前置交互预检卡片（Security Pre-flight Modal），提供**🛡️ 脱敏后发送 (推荐)**、**⚡ 原样发出 (本次豁免)** 与 **✏️ 返回修改** 的三态弹性放行能力，支持单会话决策记忆，并将豁免操作留存为不可篡改的企业合规审计日志；同步修复系统提示词与安全围栏的假阳性误伤，保障出境流式交互的流畅性与合规性。

---

## 🌟 核心痛点与演进背景 (Why v1.8.3)

在企业级落地与日常极客开发中，传统安全围栏机制暴露出严重影响研发效率的体验缺陷：
1. **事后“硬拦截”直接掐断对话**：当用户输入私有 IP（如 `192.168.1.100`）、研发测试用的虚拟密钥或内网数据库连接串时，后端往往在请求已经发出、模型已经开始思考后直接抛出异常硬阻断，甚至直接丢失用户已键入的长文本，用户必须重新措辞、重复输入，挫败感极强；
2. **静默脱敏剥离上下文导致排障失真**：部分系统采用静默脱敏（替换为 `<PROTECTED_IP>`），但在网络联通性排查、内网拓扑诊断等特定场景下，真实 IP 正是任务的核心参数，盲目替换会导致 Agent 无法准确定位问题，甚至生成错误的网络路由建议；
3. **系统内置提示词误伤自我封锁**：Agent 内置的基础提示词中若包含网络测试示例（如 `192.168.1.1`），在模型进行自我反思规划时会触发自身出境脱敏规则，提示词被篡改并覆盖了思考流，造成任务莫名停滞或无输出；
4. **安全与合规审计缺乏平衡**：安全部门需要 100% 的敏感信息审计留存，而开发人员需要合理的“临时豁免放行”机制，两者的对立导致安全工具往往被开发者彻底关闭（设置为 `disabled`），反而制造了更大的安全隐患。

---

## 🚀 v1.8.3 核心交付全景 (Core Highlights)

### 一、🛡️ 出境安全前置交互预检与三态弹性放行 (P0)
- **输入框前置毫秒级特征预检**：
  - 用户在聊天框点击发送或按下 `Enter` 时，前端基于毫秒级内存正则引擎调用 `window.electronAPI.testSanitizeText` 对完整出境文本（含文本与附件元信息）进行即时扫描；
  - 若文本不含敏感特征（0 处命中），**0ms 零感知直通发出**，保持无缝极致体验；
  - 若命中敏感资产，**自动弹出前置交互预检模态弹窗（SecurityPreflightModal）**，阻断未经确认的盲目出境。
- **三态弹性放行决议**：
  - 🛡️ **脱敏后发送 (推荐)**：自动采用高辨识度的语义占位符（如 `<PRIVATE_IP_REDACTED_1>`、`<DB_PASSWORD_REDACTED_1>`）替换敏感资产后发出，前端消息流直观展现脱敏后的真实内容，确保安全出境；
  - ⚡ **原样发出 (本次豁免)**：支持针对内网诊断、联调排障等特定场景进行单次或会话级豁免，直接原样将请求送达大模型；
  - ✏️ **返回修改**：点击关闭弹窗并自动聚焦回聊天输入框，保留用户输入内容供其自由修改。
- **当前会话偏好记忆 (Session Preference)**：
  - 弹窗底部提供「在当前会话中记住此选择」勾选框；
  - 勾选后，同一会话内的后续交互将按用户的决议直接执行脱敏或豁免，无需重复弹窗打扰；
  - 切换至新任务会话时，安全围栏自动恢复严苛预检，防止跨项目产生安全敞口。

---

### 二、📋 完备合规审计日志与豁免全生命周期追踪 (P1)
- **豁免操作不可篡改留存**：
  - 用户确认“原样发出”时，底层自动调用 `securityFenceManager.recordBypass()`；
  - 审计日志中明确记录 `bypassed: true`、触发时间戳、敏感资产分类（私有 IP、API Key、数据库凭证等）、脱敏指纹及放行原因（`用户发送前主动确认豁免放行 (Bypassed by User)`）；
  - 严格遵循最小明文留存原则，审计日志中的原始凭证同样进行 `***` 脱敏掩码处理，兼顾取证与资产保护。
- **企业合规中枢 (Compliance Center) 深度联动**：
  - 顶部导航栏「企业合规」弹窗中实时呈现包含豁免记录在内的完整出境流量安全态势。

---

### 三、⚡ 内核自纠错与思考流管道纯净隔离 (P2)
- **内置 Prompt 消除虚拟私有 IP**：
  - 彻底移除 `electron/harness-runner.ts` 系统提示词中可能引起安全围栏假阳性误报的 `192.168.1.1` 测试示例，改为标准本地回环地址 `127.0.0.1`，消除“自己阻断自己”的误伤隐患；
- **系统通知与模型思考流纯净分离**：
  - 将安全围栏提示（`🛡️ [企业安全围栏]`、`⚡ [出境安全豁免已生效]`）与模型真实思考、工具调用过程严格物理隔离；
  - 系统通告仅通过 `onSystemNotice` 渠道送入前端展示，杜绝污染大模型的结构化 JSON-RPC 工具调用上下文与回复内容；
- **空回复智能容灾重试**：
  - 强化模型调用空流拦截兜底机制，当遭遇上游链路临时中断或 0 Token 响应时，自动平滑重试或降级至标准对话线路，保障任务规划与执行 100% 推进至完成态。

---

## 📊 质量验证与测试矩阵 (Verification Matrix)

本项目已建立覆盖 12 大测试组的自动化测试套件 `scripts/verify-v1.8.3.mjs`，包含 **176 项全量测试断言，100% 全部通过**：

| 测试组编号 | 验证模块与核心特性 | 验证断言数 | 验证状态 |
|:---:|:---|:---:|:---:|
| **Test Group 1** | 纯 JS 原生 Office 排版套件 (Word 封面/目录/表格、Excel 多工作表、PPT) | 12 项 | ✅ 100% PASS |
| **Test Group 2** | 交付物可信验证门禁 (物理校验/陈旧时间戳拦截/零字节报警/认证徽标) | 12 项 | ✅ 100% PASS |
| **Test Group 3** | Windows 终端沙箱与命令执行鲁棒性 (&& 语法改写/临时脚本沙箱) | 8 项 | ✅ 100% PASS |
| **Test Group 4** | 长期持久记忆库与自我演化技能 (任务反思/技能提炼/迁移规约) | 15 项 | ✅ 100% PASS |
| **Test Group 5** | StorageHub 闭环与 D 盘优先存储 (mcp_servers/自动加载/存储配额) | 14 项 | ✅ 100% PASS |
| **Test Group 6** | 路径安全隔离与重定向 (零 C 盘泄漏/resolveSafe 强重定向) | 18 项 | ✅ 100% PASS |
| **Test Group 7** | 零外部依赖工具链 (PDF/ZIP 压缩解压/写锁自愈/Git 无依赖检测) | 19 项 | ✅ 100% PASS |
| **Test Group 8** | Skill & MCP 深度融合 (npx 注入/Schema 校验/微索引/YAML 解析/纯 JS 工具) | 33 项 | ✅ 100% PASS |
| **Test Group 9** | 步骤状态流转优化与 UI 取消失步修复 | 9 项 | ✅ 100% PASS |
| **Test Group 10** | Grill-me 多题结构化交互与问卷提取 | 8 项 | ✅ 100% PASS |
| **Test Group 11** | Word 嵌套代码块提取与静态 TOC 生成 (零 Word 告警) | 10 项 | ✅ 100% PASS |
| **Test Group 12** | **v1.8.3 出境安全前置交互预检、三态弹性放行与审计留痕** | **18 项** | **✅ 100% PASS** |
| **合计** | **全模块全链路自动化验证覆盖** | **176 项** | **✅ 100% 全部 PASS** |

---

## 📦 官方安装包与交付物 (Delivery Assets)

所有 Windows 交付安装包已完成基于 Electron 34 的静态编译与无依赖封装，输出至 `release/` 目录：

1. **Windows 独立标准安装包**：
   - 路径：`release/ASTeam Agent-Setup-1.8.3.exe`
   - 大小：127,090,947 字节 (~121.2 MB)
   - 特点：支持自定义安装路径、自动创建桌面快捷方式、内置卸载程序。

2. **Windows 单文件免安装便携版 (Green Portable)**：
   - 路径：`release/ASTeam Agent-Portable-1.8.3.exe`
   - 大小：126,760,715 字节 (~120.9 MB)
   - 特点：解压即用、零注册表残留、企业 U 盘即插即测。

---

*ASTeam Agent 架构研发团队 · 2026 年 9 月 12 日*
